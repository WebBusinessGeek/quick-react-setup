var React = require("react");

var DashboardPage = React.createClass({
    render: function() {
        return(
            <div id="dashboardPageContainer">
                <p id="dashboardPageTitle">This is the dashboard</p>
            </div>
        )
    }
});

module.exports = DashboardPage;